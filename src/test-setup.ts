// Dexie захватывает indexedDB в момент создания экземпляра базы, то есть при
// импорте core/db. Подменять его внутри spec-файла уже поздно — заглушка
// должна встать раньше любых импортов.
import 'fake-indexeddb/auto';
